rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Mailbox metadata and encrypted provider credentials are server-only.
    // Firebase Admin SDK bypasses these rules; the browser uses the API instead.
    match /{document=**} { allow read, write: if false; }
  }
}
